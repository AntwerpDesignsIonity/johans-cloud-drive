rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read/write for any signed-in user (including anonymous auth)
      allow read, write: if request.auth != null;
    }
  }
}
